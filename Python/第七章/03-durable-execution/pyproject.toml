[project]
name = "agent-course-durable-execution-python"
version = "0.1.0"
description = "Durable execution demo with LangGraph checkpoints and PostgreSQL"
requires-python = ">=3.11"
dependencies = [
    "langgraph==1.2.11",
    "langgraph-checkpoint-postgres==3.1.2",
    "psycopg[binary,pool]==3.3.5",
    "pydantic==2.13.4",
    "typing-extensions==4.15.0",
]

[build-system]
requires = ["setuptools>=80", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = [
    "durable_workflow",
    "workflow",
]
